// let age = 23 ;
// let condition = (age >=18) && (age<=50) ? "Eligible to work" : "Not eligible to work";
// console.log(condition); // Output: "Eligible to work" // logical AND operator

let age = 54 ;
let gender = 'm';
let condition = (age >=18) || (gender === 'f') ? "Eligible to work" : "Not eligible to work";
console.log(condition); // Output: "Not eligible to work" // logical OR operator


let ans = !(false)
console.log(ans) // not operator    